const fs = require("fs");
const input = fs.readFileSync("input.txt").toString().trim().split("\r\n");
let [tmp1, tmp2] = input;
let [L, C] = tmp1.split(" ");
L = Number(L);
C = Number(C);
let arr = tmp2.split(" ");

console.log(arr);
arr.sort();
console.log(arr);
function DFS(L) {
  if (L === n) {
    return;
  } else {
  }
}

DFS(0);
